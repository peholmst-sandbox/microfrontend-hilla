spring:
    application:
        name: gateway-app
    cloud:
        gateway:
            discovery:
                locator:
                    enabled: true
eureka:
    client:
        serviceUrl:
            defaultZone: http://${SPRING_EUREKA_HOST}:8761/eureka/
